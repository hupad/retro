<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Retro</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <%= form_for :search, url: search_path, html: { class: "navbar-form navbar-left", role: "search" } do |f| %>
          <div class="form-group">
           <%= f.text_field :query, class: "form-control", placeholder: "Search Retro Notes" %>
          </div>
          <%= f.submit "Search", class: "btn btn-default" %>
      <% end %>

      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "About", root_path %></li>
        <% if !user_signed_in? %>
          <li><%= link_to "Sign In", new_user_session_path %></li>
        <% else %>
          <li><%= link_to "Retro List", sretros_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> My Account <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="#">Account Settings</a></li>
              <li><a href="#">My Retro's</a></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>